Makefile for PyAngo Music App.
-------------------------------

* To create python virtualenv
  make virtualenv

Following programs must be installed and avaialble:
  mongodb

Package to be installed from pylib_req.txt:
